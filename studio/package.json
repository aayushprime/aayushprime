{
  "name": "@aayushprime/studio",
  "private": true,
  "type": "module",
  "description": "Local content editor for the Hugo site. Writes markdown into ../content and previews it through a supervised hugo server.",
  "scripts": {
    "dev": "pnpm --filter studio-server dev",
    "test": "pnpm --filter studio-server test",
    "typecheck": "pnpm --filter studio-server typecheck && pnpm --filter studio-client typecheck"
  },
  "packageManager": "pnpm@10.33.0"
}
